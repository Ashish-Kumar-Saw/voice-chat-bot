services:
  - type: web
    name: voice-ai-bot
    env: python
    buildCommand: bash build.sh
    startCommand: streamlit run bot.py --server.port $PORT --server.address 0.0.0.0
    envVars:
      - key: GEMINI_API_KEY
        sync: false
    plan: free